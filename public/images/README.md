Save the supplied galaxy wallpaper here as `universe.jpg`.

The Canvas uses it as a fixed CSS background behind the transparent 3D rendering. Until the image is provided, the dark fallback color remains visible. `cover` preserves the image proportions and crops the portrait wallpaper to fill the screen.
